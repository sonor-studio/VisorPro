import SwiftUI

struct TestView: View {
    var body: some View {
        Rectangle()
            // .subtracting is not standard API. 
            // Wait, what is the API for boolean shape operations?
    }
}
print("Done")
